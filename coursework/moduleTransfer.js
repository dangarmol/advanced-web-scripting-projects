//Transfer object for the current module information (excluding the students and groups)

var moduleCode;
var moduleName;
var moduleCohort;

function getAllModuleData() {
    var moduleData = {moduleCode, moduleName, moduleCohort};
   
    return moduleData;
}